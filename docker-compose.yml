version: '3.8'

services:
  redis:
    build:
      context: .
      dockerfile: ./docker/redis/Dockerfile
    container_name: cravo-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
